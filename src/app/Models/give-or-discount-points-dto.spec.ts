import { GiveOrDiscountPointsDto } from './give-or-discount-points-dto';

describe('GiveOrDiscountPointsDto', () => {
  it('should create an instance', () => {
    expect(new GiveOrDiscountPointsDto()).toBeTruthy();
  });
});
