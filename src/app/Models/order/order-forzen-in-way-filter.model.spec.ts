import { OrderForzenInWayFilter } from './order-forzen-in-way-filter.model';

describe('OrderForzenInWayFilter', () => {
  it('should create an instance', () => {
    expect(new OrderForzenInWayFilter()).toBeTruthy();
  });
});
