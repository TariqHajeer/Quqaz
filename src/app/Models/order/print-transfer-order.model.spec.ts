import { PrintTransferOrder } from './print-transfer-order.model';

describe('PrintTransferOrder', () => {
  it('should create an instance', () => {
    expect(new PrintTransferOrder()).toBeTruthy();
  });
});
